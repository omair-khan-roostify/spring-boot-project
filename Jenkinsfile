@Library('jenkins-shared-libraries') _
import com.john.*

//eo = new EvenOddBuilder()  
//eo.evenOrOdd(currentBuild.getNumber())
  def config = [isSonarNeeded:true]
  println("Map value is "+config.getKey('isSonarNeeded'))
  evenOrOdd(config)
